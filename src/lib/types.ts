export interface SignalBreakdown {
  name: number | boolean | string;
  address: number | boolean | string;
  pan: number | boolean | string;
  gstin: number | boolean | string;
  intra_dept: number | boolean | string;
}